#include <stdio.h>
#include<string.h>
int main()
{
char s[100];
int i,n,temp;
printf("enter the string:");
gets(s);
n=strlen(s);
int a=n-1;
for(i=0;i<a;i++)
{
    temp=s[i];
    s[i]=s[a];
    s[a]=temp;
    a--;
}
puts(s);
}
