#include <stdio.h>
#include<string.h>
int main()
{
char s[100];
int i,n;
printf("enter the string:");
gets(s);
n=strlen(s);
for(i=0;i<n/2;i++)
{
    if(s[i]==s[n-1-i])
    {
    printf("string is palindrome\n");
    }
    else
    {
    printf("string is not palindrome");
    }
}
}
